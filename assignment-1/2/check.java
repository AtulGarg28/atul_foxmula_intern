class check{
	static boolean checkIfEqual(Main m1,Main m2){
        	if(m1.count()==m2.count()){
       	     		System.out.println("Both stacks are equal with size of "+m1.count());
    			return true;
        	}
        return false;
    	}
}